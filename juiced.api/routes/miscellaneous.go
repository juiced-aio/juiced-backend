package routes

import (
	"github.com/gorilla/mux"
)

// RouteCheckoutsEndpoints routes endpoints that handle miscellaneous things
func RouteMiscellaneousEndpoints(router *mux.Router) {
	// router.HandleFunc("/api/misc/webhook/test", endpoints.TestWebhooksEndpoint).Methods("POST")
	// router.HandleFunc("/api/misc/version", endpoints.SetVersion).Methods("POST")
}
